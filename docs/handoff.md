# Session Handoff (docs/handoff.md)

> **運用ルール**
> - セッション間の作業状態の引き継ぎファイルです。
> - セッション開始時 (`In`) に読み込み、セッション終了時 (`Out`) に更新します。
> - 恒久的な設計情報はここではなく `docs/design.md` に、案件のゴールは `spec.md` に書きます。

---

## 0. 次のセッションが最初に読むべきこと (READ FIRST)

**この案件の依頼者は非エンジニアです。コードを1行も読めません。** 次の担当は Claude とは限りません（Codex / Antigravity も同じリポジトリを触ります）。

### 禁止事項
1. **エンジニア向け語彙で話さない。** 技術用語は使うが、必要なら定義を添える（AGENTS.md D-2）。
2. **変更範囲をユーザーに技術用語で指定させない。**
3. **技術的な選択肢を並べて選ばせない**（評価にコード知識が要るため）。
4. **手が止まっていないのに確認を投げない。** 確認は `spec.md` を決めるときの二択1回に限る。
5. **規模・頻度・回数を問われたら、まず桁を出す。** 前提を置いてでも数字を先に。
6. **PRを出したら次の作業に移る。** 定期的に様子を見に行かない。

### まず読むべきもの
- **[`AGENTS.md`](../AGENTS.md) 冒頭** = 望ましい未来1件・避けるべき未来5件
- **[`.specify/memory/constitution.md`](../.specify/memory/constitution.md)** = 採用手法6段・着手判定4項目・工程の順序（Spec Kit の実行時に効く原則）
- **[`docs/design.md`](./design.md) 第4節** = 何を採用し、何を作らないと決めたか（一次資料つき）

**このリポジトリは案件テンプレート本体です。** ルートの `spec.md` は雛形なので、実案件の内容で埋めないでください。案件の仕様は `specs/<feature>/spec.md` に置きます。

---

## 1. 今回やったこと (Completed in this session)

**2026-08-28: 工程を Spec Kit へ載せ替え、実案件を1本、実装まで通し、単一AIと比較しました。**

1. **自作の要件定義・分解をやめ、GitHub Spec Kit（公式 OSS）へ載せ替え**（Claude / Codex / Antigravity の3種）。既存の追跡ファイルは1つも書き換わっていません
2. **⑥ Graph（実行側）を公式機構の選択として決定**。既定は逐次、独立作業がまとまってあるときは単位ごとの隔離、多数の対象は workflow。Agent teams と自作の実行基盤は使いません
3. **案件001（この工程そのもの）を仕様から実装まで通しました。** 門①は1回目「違う」→ 書き直し → 2回目「そう」
4. **案件002（営業日を数えるコマンド）を実装し、測定対象にしました。** 独立2単位を隔離して実装し、片方だけを落として局所再実行を実測
5. **単一AIとの比較を記録しました。** [`docs/measurements/README.md`](./measurements/README.md)

## 2. 現在の状態 (Current State)

### 成立したこと

- **失敗の局所化**: 赤 / 緑 / 戻して赤を実測。緑の単位はコミット数1件のまま変化せず、作り直し0件。統合時の衝突0件。使ったのは git だけで、新しい実行基盤を1行も書いていません
- **機械判定のみで解放**: 次工程を解放した4箇所すべてが検査の終了コード。AI の判断による解放は0件
- **成果物**: `sandbox/business-days/` が動きます。単体テスト25件、受入検査5件すべて緑

### 都合の悪い結果（隠さず残しています）

**この規模では、構造化した実行が単一AIより良い結果を出していません。** 受入検査5件はどちらも一発通過。検収条件の外側を当てても、どちらも黙って数値を返さずに止まりました。到達点は同じで、構造化した側は明らかに多くの時間を使いました。

**最も知りたかったトークンの比較ができていません。** 構造化した側の消費を分離して取る手段がありません。

### やっていないこと（正確に）

- `/batch` の実走（このセッションでスキルとして提供されていなかった）
- 単位ごとの pull request と、CI 上での検査実行（ブランチ規約のため）
- プロセスを強制終了しての再開

## 3. 次回やること (Next Steps)

1. **単一AIの一度の作業に収まらない大きさの対象で、同じ測定をやり直す。** 今回の1件は小さすぎて、構造化の利点（成功した仕事を残して失敗箇所だけ修復する）が意味を持ちませんでした。境目はこの1件からは分かりません
2. **トークン消費を単位ごとに取る手段を決める。** 現状は手作業の転記すらできません。これが取れないと、この方式を採り続ける根拠を数字で示せません
3. `/batch` と、単位ごとの pull request / CI を実際に走らせる（今回はセッションの制約で未実施）
